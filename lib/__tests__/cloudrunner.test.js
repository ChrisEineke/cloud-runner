const assert = require('assert');
const cloudrunner = require('../index.js');

describe('cloudrunner', function () {
  it('has a test', function () {
    assert(false, 'cloudrunner should have a test');
  });
});
